<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codeontime.munna">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuizBook">
        <activity android:name=".View.QuestionsList" android:noHistory="true"></activity>
        <activity android:name=".View.QuestionAdd" />
        <activity android:name=".View.ContestDetails" />
        <activity android:name=".View.ContestAdd" />
        <activity android:name=".View.Contests" />
        <activity android:name=".View.BookAdd" />
        <activity android:name=".LoginRegistration" />
        <activity android:name=".LoginProfile" />
        <activity
            android:name=".LoginCheck"
            android:noHistory="true" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>